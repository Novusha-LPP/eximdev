# ─── Mystique AI Service — Python Dependencies ─────────────────
# services/mystique/requirements.txt

# Core framework
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.9.*
pydantic-settings==2.6.*

# MongoDB (read-only access for tool execution)
motor==3.6.*                   # async MongoDB driver
pymongo==4.9.*

# LLM Providers
openai==1.55.*                 # OpenAI-compatible API (works with vLLM, Ollama)
ollama==0.4.*                  # Ollama Python SDK (direct)
# vllm                         # Uncomment if running vLLM in-process
# llama-cpp-python             # Uncomment for CPU-only llama.cpp

# Tool-use & orchestration
httpx==0.28.*                  # async HTTP client

# Fuzzy matching (duplicate detection)
thefuzz==0.22.*                # fuzzy string matching (levenshtein)
python-Levenshtein==0.26.*     # C extension for speed

# Utilities
python-dotenv==1.0.*
structlog==24.4.*              # structured logging

# Testing
pytest==8.3.*
pytest-asyncio==0.24.*
httpx                          # already listed — used as test client
